#include "main.h"
#include <string.h>
#include <stdio.h>

/**
* _strcmp - compares two strings
* @s1: string
* @s2: string
* Return: (i)
*/

int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
