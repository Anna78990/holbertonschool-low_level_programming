#include "main.h"
#include <ctype.h>

/**
 * _isupper - it checks for uppercase character.
 * @c: third integer
 * Return: (1), (0)
 */
 
int _isupper(int c)
{
if (isupper(c)){
return (1);
}
else
{
return (0);
}
}
