#include "main.h"

/**
 * is_prime_number - return 1 if it is a prime number,
 * @n: value to check
 * Return: (1)(x)(-1)
 */

int is_prime_number(int n)
{
	int i;
	i = 2;

	if ((n != 2) && (n % 2) == 0)
	{
		return 0;
	}
	else
	{
		return ;
	}
}
